# Dependencies
node_modules/
frontend/node_modules/

# Build outputs
frontend/dist/
site/

# Generated API schema dumps (local inspection only — never committed)
openapi.json

# IDE & local tooling
.idea/
.vscode/
.run/
.claude/
CLAUDE.md
**/CLAUDE.md
*.iml

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.production

# Uploads & data
uploads/
data/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Excalidraw working files
docs/excalidraw/

# Logs
*.log
hs_err_pid*

# Docker
docker-compose.override.yml

# Audit profiles (internal tooling)
profiles/

# E2E tests — Maven build outputs & Chrome user data
e2e/**/target/

# Local-only artifacts (release/0.6.0 bootstrap)
.github/ISSUE_TEMPLATE/issue.md
docs/claude-code-commands.md
docs/design/TEMPLATE.md
docs/git-workflow/autonomy-mode.md


# Demo capture artefacts (scripts/demo) — raw video, narration audio, timing logs.
# Regenerable, and a 4-minute screen capture has no business in git.
out/
e2e/ui/src/test/resources/demo/pace.json

# Local scratch dropped at the repo root — reference papers, and the stub
# lockfile a stray `npm install` leaves behind (there is no root package.json).
# Root-scoped so fixtures in subdirectories are unaffected.
/*.pdf
/package-lock.json
